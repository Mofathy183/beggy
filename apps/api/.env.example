# ---------------------------
#* Application Identity
# ---------------------------
# REQUIRED: Unique application name used for cookie naming and identification
# - Must be lowercase, alphanumeric with underscores
# - Used as prefix for all application-specific cookies
# - Helps prevent cookie name collisions between different apps on same domain
# - Example: If APP_NAME=beggy, cookies become: beggy_access_token, beggy_refresh_token
APP_NAME=beggy

# ---------------------------
#* Environment
# ---------------------------
# Set "development", "production", or "test"
NODE_ENV=development

# Port your Express server will run on
PORT=3000

# ---------------------------
#* Authentication - Access Token
# ---------------------------
# Generate using: openssl rand -base64 64
JWT_SECRET=your_jwt_secret_here
# Access token lifetime (ex: "15m", "1h", "7d")
JWT_EXPIRES_IN=15m
# - Overrides default naming: ${APP_NAME}_access_token
# - Use unique name to prevent detection by malicious scripts
# - Can include versioning for seamless updates (e.g., beggy_v2_access)
JWT_ACCESS_TOKEN_NAME=beggy_access_token

# ---------------------------
#* Authentication - Refresh Token
# ---------------------------
# Generate using: openssl rand -base64 64
JWT_REFRESH_SECRET=your_refresh_jwt_secret_here
# Refresh token lifetime
JWT_REFRESH_EXPIRES_IN=7d
# - Overrides default naming: ${APP_NAME}_refresh_token
# - Use obscure names to avoid detection
# - Example: beggy_persistence_token
JWT_REFRESH_TOKEN_NAME=beggy_refresh_token


# ---------------------------
#* Session Security (for express-session)
# ---------------------------
# Generate using: openssl rand -base64 64
SESSION_SECRET=your_session_secret_here

# ---------------------------
#* Password Hashing
# ---------------------------
# Recommended cost: 10–12 in production
BCRYPT_SALT_ROUNDS=10

# ---------------------------
#* Database URL
# ---------------------------
DATABASE_URL="postgresql://username:password@localhost:5432/mydb?schema=public"

# ============================================================================
#* CSRF PROTECTION CONFIGURATION (Double Submit Cookie Pattern)
# ============================================================================

# ---------------------------
#* CSRF Secret Key
# ---------------------------
# REQUIRED: Cryptographic secret for token generation
# Generate using: openssl rand -base64 32
# Must be at least 32 characters
CSRF_SECRET_KEY=your_csrf_secret_here

# ---------------------------
#* CSRF Token Configuration
# ---------------------------
# OPTIONAL: Length of CSRF token in bytes
# Recommended: 64 (produces 128-character hex token)
# Range: 32-128 bytes
CSRF_TOKEN_LENGTH=64

# ---------------------------
#* CSRF Cookie Configuration
# ---------------------------
# OPTIONAL: Name of the CSRF cookie
# Use __Host- prefix in production for additional security, like: __Host-psifi.x-csrf-token
# Default: XSRF-TOKEN
CSRF_COOKIE_NAME=XSRF-TOKEN
